import React from "react";
// import { motion } from "@motionone/react";
import { motion, scale } from "framer-motion";

export default function App() {
  return <motion.ul animate={{ rotate: 360 }} />;
}
